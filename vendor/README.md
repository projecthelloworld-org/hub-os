# Upstream Compose applications

This directory is populated by `make ecosystem-setup` and is otherwise ignored by Git.

HubOS pins the official ODK Central and Headwind MDM repositories instead of copying their internal Compose services. This preserves their supported layouts and makes their versions and upgrade procedures explicit.
